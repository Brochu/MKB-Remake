using UnityEngine;
using System.Collections;

public class Player : MonoBehaviour {
    // this will be used to keep player related data (like current level) for each player in multiplayer mode
    // Use this for initialization
    void Start () {
    
    }
    
    // Update is called once per frame
    void Update () {
    
    }
}
